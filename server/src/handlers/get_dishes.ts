import { type Dish } from '../schema';

export async function getDishes(): Promise<Dish[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all available dishes with their variants from the database.
    // Should include filtering by status (available only for customers) and include stock information.
    return [];
}